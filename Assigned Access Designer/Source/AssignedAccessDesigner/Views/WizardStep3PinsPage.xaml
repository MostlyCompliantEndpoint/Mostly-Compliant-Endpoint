<Page
    x:Class="AssignedAccessDesigner.Views.WizardStep3PinsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:AssignedAccessDesigner.ViewModels">
    
    <Page.Resources>
        <DataTemplate x:Key="PickerItemTemplate" x:DataType="vm:AppxPackage">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="{x:Bind DisplayName}" />
                <TextBlock Text="{x:Bind FullName}" Foreground="Gray" FontSize="12" />
                <TextBlock Text="{x:Bind PublisherDisplayName}" Foreground="Gray" FontSize="12" />
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Start Menu" Style="{StaticResource TitleTextBlockStyle}" />

            <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Left" Spacing="8" Margin="0,12,0,0" Padding="0,12,0,0">
                <Button x:Name="AddUWP" Content="Add UWP" Click="AddUwpApps_Click" />
                <Button x:Name="AddExplorer" Content="Add From Explorer" Click="AddFromExplorer_Click" />
                <Button Content="Add Manually" Click="Add_Click"/>
                <Button Content="Remove Selected" Click="Remove_Click"/>
            </StackPanel>

            <ListView x:Name="PinsList" Grid.Row="2" SelectionMode="Single" ScrollViewer.HorizontalScrollMode="Auto">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:StartMenuPin" xmlns:models="using:AssignedAccessDesigner.Models">
                        <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,8,0,8">
                            <ComboBox x:Name="ProfileCombobox" ItemsSource="{x:Bind ProfileListIds}"  Header="Profile ID" SelectedValue="{x:Bind ProfileId, Mode=TwoWay}"/>
                            <TextBox Header="AUMID / Path" LostFocus="Path_LostFocus" MinWidth="600" Text="{x:Bind Path, Mode=TwoWay}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        
        
    </Grid>
</Page>
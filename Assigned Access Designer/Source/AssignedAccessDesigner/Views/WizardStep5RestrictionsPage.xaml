<Page
    x:Class="AssignedAccessDesigner.Views.WizardStep5RestrictionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Device Restrictions" Style="{StaticResource TitleTextBlockStyle}" />

            <ScrollViewer x:Name="ScrollViewerRestrict" Grid.Row="2"  VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="RestrictList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="models:ExplorerRestriction" xmlns:models="using:AssignedAccessDesigner.Models">

                            <StackPanel Padding="24" Spacing="12">

                                <StackPanel Orientation="Vertical" Spacing="16">

                                    <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Stretch">
                                        <NumberBox Header="Profile ID" IsEnabled="false" x:Name="RestrictionProfile" Text="{x:Bind ProfileId, Mode=TwoWay}"/>
                                        <ToggleSwitch Header="Restrict All" IsOn="{x:Bind RestrictAll, Mode=TwoWay}"/>
                                        <ToggleSwitch Width="150"  Header="Allow Downloads Folder" IsOn="{x:Bind AllowDownloads, Mode=TwoWay}"/>
                                        <ToggleSwitch Width="150"  Header="Allow Removable Drives" IsOn="{x:Bind AllowRemovableMedia, Mode=TwoWay}"/>
                                        <ToggleSwitch Width="100"  Header="No Restrictions" IsOn="{x:Bind NoRestrictions, Mode=TwoWay}"/>
                                    </StackPanel>

                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</Page>